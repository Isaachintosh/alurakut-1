import styled from 'styled-components';

export const Container = styled.div`
  position: relative;
  padding-top: 32px;

  .button-close {
    position: absolute;
    top: 16px;
    right: 16px;

    background: transparent;
    border: 0;
  }
`;
